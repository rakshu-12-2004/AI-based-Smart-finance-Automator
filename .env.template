# Smart Personal Finance Automator - Environment Configuration

# =============================================================================
# DATA SOURCE MODE
# =============================================================================
# Choose how transaction data is loaded:
#   - 'sample' : Use demo/test data (no Gmail required)
#   - 'gmail'  : Read from real Gmail account (requires setup)
#
# Default: sample
DATA_SOURCE_MODE=sample

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Flask secret key (change this in production!)
SECRET_KEY=your-secret-key-change-me-in-production

# Flask debug mode (set to False in production)
FLASK_DEBUG=True

# Application port
FLASK_PORT=5001

# =============================================================================
# DATABASE SETTINGS
# =============================================================================
# Database path (SQLite by default)
DATABASE_PATH=database/finance_automator.db

# =============================================================================
# GMAIL API SETTINGS (Only needed for Gmail mode)
# =============================================================================
# Gmail API credentials file path
GMAIL_CREDENTIALS_FILE=credentials.json

# Gmail token file path (auto-generated on first auth)
GMAIL_TOKEN_FILE=gmail_token.pickle

# Default days to look back for emails
GMAIL_DAYS_BACK=7

# Maximum emails to process per sync
GMAIL_MAX_RESULTS=100

# =============================================================================
# NLP SETTINGS
# =============================================================================
# Minimum confidence score for transaction extraction (0.0 - 1.0)
NLP_MIN_CONFIDENCE=0.6

# Enable spaCy NLP (requires spaCy model installed)
NLP_SPACY_ENABLED=False

# =============================================================================
# NOTES
# =============================================================================
# 1. Copy this file to .env in the project root
# 2. Modify values as needed
# 3. Never commit .env to version control (it's in .gitignore)
# 4. For Gmail mode, you'll also need credentials.json from Google Cloud Console
